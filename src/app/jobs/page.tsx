"use client"
import { supabase } from "@/lib/supabase";
import { useState } from "react"

const Page = () => {

    const [state, setState] = useState<any>();
    return (

    <div>
        hello world
    </div>
    )
}

export default Page